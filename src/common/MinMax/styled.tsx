import Input from 'common/Input';
import styled from 'styled';

export const MinMaxInput = styled(Input)`
  width: 42px;
  font-size: .875em;
`;

export const StyledMixMax = styled.div`
  display: flex;
`;
